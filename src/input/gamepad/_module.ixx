/**
 * @file _module.ixx
 * @brief Aggregate module for gamepad input handling and configuration.
 */
export module helios.engine.input.gamepad;

export import helios.engine.input.gamepad.GamepadState;
export import helios.engine.input.gamepad.GamepadSettings;
export import helios.engine.input.gamepad.DeadzoneStrategy;
export import helios.engine.input.gamepad.RadialDeadzoneStrategy;
