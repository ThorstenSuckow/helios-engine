/**
 * @file _module.ixx
 * @brief Aggregate module for gamepad input handling and configuration.
 */
export module helios.input.gamepad;

export import helios.input.gamepad.GamepadState;
export import helios.input.gamepad.GamepadSettings;
export import helios.input.gamepad.DeadzoneStrategy;
export import helios.input.gamepad.RadialDeadzoneStrategy;
