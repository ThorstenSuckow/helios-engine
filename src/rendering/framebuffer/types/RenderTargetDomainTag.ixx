/**
 * @file RenderTargetDomainTag.ixx
 * @brief Domain tag used for renderTarget IDs and handles.
 */
module;

export module helios.engine.rendering.renderTarget.types.RenderTargetDomainTag;

export namespace helios::engine::rendering::renderTarget::types {

    /**
     * @brief Domain tag for renderTarget identity and handles.
     */
    struct RenderTargetDomainTag{};

}

