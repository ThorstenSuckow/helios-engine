# helios::runtime::world::types

Type aliases and type-id utilities for the runtime-world domain.

## Types

| Type | Purpose |
|------|---------|
| `GameObjectDomainTag` | Shared domain tag for game-object IDs and handles |
| `GameObjectId` | Strong id used by runtime game-object handles |
| `GameObjectHandle` | Typed entity handle for runtime game objects |
| `ResourceTypeId` | Type index id for resource-registry entries |
| `ManagerTypeId` | Type index id for manager categories |
| `SystemTypeId` | Type index id for system-registry entries |

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::runtime::world::types
@brief Type aliases and type-id utilities for the runtime-world domain.
</p></details>

