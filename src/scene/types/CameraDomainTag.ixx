/**
 * @file CameraDomainTag.ixx
 * @brief Domain tag used for camera IDs and handles.
 */
module;

export module helios.engine.scene.types.CameraDomainTag;

export namespace helios::engine::scene::types {

    /**
     * @brief Domain tag for camera identity and handles.
     */
    struct CameraDomainTag{};

}

